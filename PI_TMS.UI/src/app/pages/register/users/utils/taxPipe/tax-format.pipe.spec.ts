import { TaxFormatPipe } from './tax-format.pipe';

describe('TaxFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TaxFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
